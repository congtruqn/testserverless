service: my-express-application

provider:
  name: aws
  custom:
    xyzsecret: ${ssm:/aws/reference/secretsmanager/testsecrect~true}
  runtime: nodejs12.x
  stage: dev
  region: ap-southeast-1
  tracing:
    lambda: true
  environment:
    APP_DOMAIN: ${ssm:parameterStore}
    AWS_SECRET_ID: ${ssm:/aws/reference/secretsmanager/testsecrect~true}.user
functions:
  app:
    handler: index.handler
    events:
      - http: ANY /
      - http: 'ANY {proxy+}'               