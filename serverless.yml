service: my-express-application

provider:
  name: aws
  custom:
    supersecret: ${ssm:/aws/reference/secretsmanager/AWS_SECRET~true}
  runtime: nodejs12.x
  stage: dev
  region: ap-southeast-1
  tracing:
    lambda: true
  environment:
    APP_DOMAIN: ${ssm:parameterStore}
    AWS_SECRET_ID: ${self:custom.supersecret}
functions:
  app:
    handler: index.handler
    events:
      - http: ANY /
      - http: 'ANY {proxy+}'               