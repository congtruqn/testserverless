version: 0.2

phases:
  install:
    commands:
      - echo install serverless framework
      - npm install -g serverless
  pre_build:
    commands:
      - echo install pre build commands
      - npm install
  build:
    commands:
      - echo deployment started `date`
      - echo deploy serverless framework custom codebase
      - serverless config credentials --provider aws --key AKIA6D3AO5J2UVT4R5V4 --secret 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
      - serverless deploy --stage dev
  post_build:
    commands:
      - echo post build completed on `date`