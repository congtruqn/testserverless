version: 0.2

phases:
  install:
    commands:
      - echo install serverless framework
      - npm install -g serverless
  pre_build:
    commands:
      - echo install pre build commands
      - npm install
  build:
    commands:
      - echo deployment started `date`
      - echo deploy serverless framework custom codebase
      - serverless config credentials --provider aws --key AKIA6D3AO5J2TM5IMG72 --secret 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
      - serverless deploy --stage dev --region ap-southeast-1
  post_build:
    commands:
      - echo post build completed on `date`